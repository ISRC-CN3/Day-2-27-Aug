function [Q, pol] = VI(M)
% This function computes the optimal Q-function for the MDP M.
% It returns the optimal Q-function and the corresponding (optimal) policy

init;

% Initialize Q as a matrix of state x action
Q = ???;

% Value iteration loop
quit = 0;
while (~quit)
    
    % Update the Q function
    Q = ???;
    
    % If the update is small enough, we stop now
    if ???
        quit = 1;
    end
end

% Compute the corresponding policy
pol = ???;
